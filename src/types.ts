export type ServiceData = {
  name: string;
  duration: number;
  price: number;
  bookingsCount: number;
};

export type ProfessionalData = {
  fullName: string;
  specialty: string;
  bio: string;
};
